{
  "name": "github-scripts",
  "version": "0.0.1",
  "description": "Automation scripts using the GitHub API.",
  "type": "module",
  "scripts": {
    "postinstall": "git submodule update --init --recursive --remote",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ben Elan",
  "license": "MIT",
  "dependencies": {
    "@octokit/plugin-throttling": "^3.6.2",
    "@octokit/rest": "^18.12.0"
  }
}
